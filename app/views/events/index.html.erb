<h1> All events</h1> 
<h1>Example heading <span class="badge badge-secondary">New</span></h1>
<ul>
	<% for event in @events %>
		<li><%= link_to event.artist, "/events/#{event.id}" %></li>
	<% end %>
</ul>
<%= link_to 'Nowe new event', events_new_path %>